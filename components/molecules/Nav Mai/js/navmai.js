(function($) {
	'use strict';
	/**
	 * Nav Mai module implementation.
	 *
	 * @author ernscht <ernscht@gmail.com>
	 * @namespace T.Module
	 * @class NavMai
	 * @extends T.Module
	 */
	T.Module.NavMai = T.createModule({
		start: function(resolve) {
			var $ctx = $(this._ctx);

			resolve();
		}
	});
}(jQuery));
